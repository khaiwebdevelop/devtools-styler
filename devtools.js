/* var xhr = new XMLHttpRequest();
xhr.open("GET", "/styles.css", false);
xhr.send();
chrome.devtools.panels.applyStyleSheet(xhr.responseText); */

function main() {
  'use strict';
  // TODO: load a theme by the information stored in chrome.store
}
